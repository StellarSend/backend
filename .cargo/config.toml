# Override the parent workspace's wasm32 default target.
# The backend is a native server binary — always build for the host.
[build]
# Override the parent workspace's wasm32 default target.
# The backend is a native server binary — build for the host by default.
